apiVersion: networking.istio.io/v1beta1
kind: VirtualService
metadata:
  name: soft-pos-service
  namespace: card-processing
spec:
  hosts:
    - dev-rtp-proxy.unibank.lan
  gateways:
    - rtp-proxy-gateway
  http:
    - name: "pos-soft-vs"
#      match:
#        - uri:
#            prefix: "/SoftPos"
#      rewrite:
#        uri: "/"
      route:
        - destination:
            host: proxy-rtp-service.card-processing.svc.cluster.local
            port:
              number: 8080